# 動作確認が少し面倒

以下のような修正で、動作確認を簡便にできる可能性が高い

- AIのシステムプロンプトを変更可能（少なくともローカルでは）
    - 現状、ちょっとした機能確認のためにチャットするときに、毎回AIが不必要に長い回答を返す
    - 原因は、システムプロンプトで複雑なプロンプトを指定しているため
- そもそもAIを使わずエコーで、ユーザープロンプトを返すバージョンも用意したい。