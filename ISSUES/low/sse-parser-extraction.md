# SSEパーサーのユーティリティ切り出し

## 概要

`index.vue` 内のSSEパース処理（約30行）を再利用可能なユーティリティに切り出す。

## 現状

- SSEパース処理は `index.vue` の `sendMessage` 内にインライン記述
- 使用箇所が1か所のみのため、ver4 で意図的に先送りした

## 対応タイミング

- SSEを利用するページ・機能が追加された時点で着手する
- `index-vue-composable` イシューと同時に対応するのが自然
