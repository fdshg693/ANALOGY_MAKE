# wrap_up SKILL にコミットステップを追加する提案

## 背景

ver14.1 のレトロスペクティブで提案し、ver14.2 でも再度確認されたが、未適用のまま。

## 問題

5ステップ中、コミットを行うのは `/imple_plan`（ステップ2）と `/retrospective`（ステップ5）のみ。
ステップ3（wrap_up）〜4（write_current）の変更はステップ5まで未コミットのまま残り、セッション中断やエラーで変更が失われるリスクがある。

## 変更内容

`.claude/skills/wrap_up/SKILL.md` の末尾に以下を追加:

```markdown
## 5. Git にコミットする

- wrap_up で行った変更（コード修正・MEMO.md 更新・ISSUES 整理等）をコミットする
- コミットメッセージ例: `docs(ver{バージョン番号}): wrap_up完了`
```

## 承認をお願いします

この変更を適用してよいか、ご確認ください。
