# MEMO: util ver3.2

## 実装メモ

- IMPLEMENT.md の計画との乖離なし。全関数・全テストを計画通りに実装。
- テスト結果: 70テスト全パス（新規20テスト含む）
- typecheck: vue-router volar 既知警告のみ（既知問題、影響なし）

## 残課題

なし

## 補足

- `FEEDBACKS/` ディレクトリはまだ存在しない状態。ユーザーが初めてFBファイルを配置した時点で機能が有効になる（ディレクトリ不在時は空リストを返すため、既存動作に影響なし）
- `FEEDBACKS/` の `.gitignore` 追加は IMPLEMENT.md の方針に従い本バージョンでは行わない
